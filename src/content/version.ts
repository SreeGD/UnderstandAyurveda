/**
 * Bumped whenever assessment questions, option weights, or scoring thresholds
 * change — anything that could make a stored result no longer reproducible.
 *
 * Lesson prose and reference edits do NOT require a bump: they cannot invalidate
 * a stored result. Stored results record the version they were produced under so
 * an older result still renders, with a note (FR-046).
 */
export const contentVersion = '2026.08.01'
